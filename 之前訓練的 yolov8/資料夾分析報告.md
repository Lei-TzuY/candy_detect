# 之前訓練的 yolov8 資料夾分析報告

## 📊 資料夾結構與用途

### 1. **dataset/** (訓練數據集) - 25.51 MB
**用途**: YOLOv8 訓練用的標準格式數據集
```
dataset/
├── images/
│   ├── train/  (450 張 .jpg) - 訓練集圖片
│   └── val/    (112 張 .jpg) - 驗證集圖片
└── labels/
    ├── train/  (450 個 .txt) - 訓練集標註 (YOLO 格式)
    └── val/    (112 個 .txt) - 驗證集標註 (YOLO 格式)
```
- ✅ **這是實際用於訓練的數據集**
- 8:2 分割比例 (450 train / 112 val)
- 已轉換為 YOLO 格式

### 2. **abnormal/** (原始瑕疵標註) - 1.63 MB
**用途**: 原始的瑕疵樣本 (XML 格式標註)
```
abnormal/
├── 35 張 .jpg 圖片
└── 35 個 .xml 標註 (VOC/PascalVOC 格式)
```
- ⚠️ **這是原始數據，未用於訓練**
- XML 格式需要轉換才能用於 YOLO

### 3. **normal/** (原始正常標註) - 24.07 MB
**用途**: 原始的正常樣本 (XML 格式標註)
```
normal/
├── 527 張 .jpg 圖片
└── 527 個 .xml 標註
```
- ⚠️ **這也是原始數據，未用於訓練**

### 4. **train/** (原始混合數據) - 25.7 MB
**用途**: 混合了正常+瑕疵的原始數據
```
train/
├── 562 張 .jpg 圖片
└── 562 個 .xml 標註
```
- ⚠️ **原始數據來源**
- 包含 abnormal (35張) + normal (527張) = 562張

### 5. **runs/** (訓練結果) - 18.31 MB
**用途**: YOLOv8 訓練輸出結果
```
runs/detect/
├── train/   - 第一次訓練結果
└── train2/  - 第二次訓練結果 ✅ 目前使用的模型
    └── weights/
        └── best.pt  - **最佳模型權重**
```
- ✅ **train2/weights/best.pt 是當前使用的模型**

### 6. **yolov8/** (配置文件) - 6.63 MB
**用途**: YOLOv8 相關配置和依賴
```
yolov8/
├── data_abnormal.yaml  - 數據集配置
├── voc2yolo_split.py   - VOC 轉 YOLO 格式腳本
├── requirements.txt    - Python 依賴
└── yolov8n.pt         - YOLOv8 預訓練模型
```

### 7. **根目錄檔案**
- `yolov8n.pt` - YOLOv8 Nano 預訓練模型 (基礎模型)
- `指令歷程記錄.txt` - 訓練命令歷史記錄

---

## 🔄 資料流程重建

根據分析，訓練流程為：

```
1. 原始標註 (XML 格式)
   ├── abnormal/ (35 張瑕疵)
   ├── normal/ (527 張正常)  
   └── train/ (562 張混合)

2. 格式轉換 (使用 voc2yolo_split.py)
   └── 轉換成 YOLO 格式 → dataset/

3. 訓練
   ├── 使用 yolov8n.pt 作為預訓練模型
   ├── 第一次訓練 → runs/detect/train/
   └── 第二次訓練 → runs/detect/train2/ ✅ (最佳)

4. 結果
   └── runs/detect/train2/weights/best.pt
```

---

## ⚠️ 重複檔案分析

**發現 531 組重複檔案！**

### 重複模式：
每個圖片都存在於 **3 個位置**：
1. `dataset/images/train/` - 訓練用
2. `abnormal/` 或 `normal/` - 原始分類
3. `train/` - 原始混合

### 示例：
```
abnormal1_jpg.rf.2a23f2a4ca431ea4cac4dcbc351127f7.jpg
├── dataset/images/train/  ✅ 實際使用
├── abnormal/              ❌ 備份/原始
└── train/                 ❌ 備份/原始
```

### 重複統計：
- **dataset**: 562 張（450 train + 112 val）
- **train**: 562 張（與 dataset 完全重複）
- **abnormal**: 35 張（子集）
- **normal**: 527 張（子集）

**總計**: 約 1686 個圖片檔案，但實際只有 **562 張不重複圖片**

---

## 💾 空間浪費

| 資料夾 | 大小 | 狀態 | 建議 |
|--------|------|------|------|
| dataset/ | 25.51 MB | ✅ 必要 | 保留 |
| abnormal/ | 1.63 MB | ❌ 重複 | 可刪除 |
| normal/ | 24.07 MB | ❌ 重複 | 可刪除 |
| train/ | 25.7 MB | ❌ 重複 | 可刪除 |
| runs/ | 18.31 MB | ✅ 必要 | 保留 |
| yolov8/ | 6.63 MB | ✅ 必要 | 保留 |

**可節省空間**: 約 51.4 MB

---

## 📝 訓練歷史

從 `指令歷程記錄.txt` 看到的訓練過程：

1. **環境設置**:
   ```bash
   conda create -n yolov8 python=3.10
   pip install ultralytics opencv-python matplotlib
   ```

2. **數據轉換**:
   ```bash
   python voc2yolo_split.py --src train --out ./dataset --mode binary --val 0.2
   ```
   - 將 XML 格式轉為 YOLO 格式
   - 80%訓練集 / 20%驗證集

3. **訓練**:
   ```bash
   yolo detect train data=dataset/data_abnormal.yaml model=yolov8n.pt epochs=50 imgsz=640 batch=16
   ```

4. **驗證**:
   ```bash
   yolo detect val model="runs/detect/train2/weights/best.pt" data="dataset/data_abnormal.yaml"
   ```

---

## 🎯 關鍵發現

### ✅ 優點：
1. 數據集結構規範（YOLO 標準格式）
2. 有完整的訓練/驗證分割
3. 保留了完整的訓練記錄和結果

### ⚠️ 問題：
1. **嚴重的檔案重複** - 同一圖片存 3 份
2. **空間浪費** - 51.4 MB 重複數據
3. **混亂的資料夾結構** - 多個用途不明確的備份
4. **標註錯誤** - abnormal 圖片被標記為 class 0（正常），應該是 class 1（瑕疵）
5. **類別嚴重不平衡** - 93.8% normal vs 6.2% abnormal

### 📌 建議：
1. **保留**：`dataset/`, `runs/`, `yolov8/`, `yolov8n.pt`
2. **刪除**：`abnormal/`, `normal/`, `train/`（都是重複的原始檔案）
3. **修正標註**：執行 fix_old_dataset_labels.py 對調類別
4. **遷移**：已將 `dataset/` 複製到主專案的 `datasets/extracted_frames/old dataset/`

---

## 🔍 數據集特性

### 類別分佈：
- **Abnormal (瑕疵)**: 35 張 (6.2%)
  - train: 29 張
  - val: 6 張
- **Normal (正常)**: 527 張 (93.8%)
  - train: 421 張
  - val: 106 張

⚠️ **嚴重的類別不平衡！**
- 這解釋了為什麼模型可能偏向預測 normal
- 建議增加更多瑕疵樣本或使用數據增強

### 圖片特性：
- 格式：JPG (黑白圖片)
- 數量：562 張不重複
- 分割：450 train / 112 val (80/20)

---

## 📊 總結

這個資料夾是一個 **YOLOv8 訓練專案**，包含：
- ✅ 完整的訓練數據集（YOLO 格式）
- ✅ 訓練好的模型（runs/detect/train2/weights/best.pt）
- ⚠️ 大量重複的原始數據備份
- ⚠️ 類別嚴重不平衡（瑕疵樣本太少）
- ⚠️ 標註錯誤（abnormal 標記為 class 0）

**當前使用的模型**: `runs/detect/train2/weights/best.pt`
**數據集規模**: 562 張圖片（450 train + 112 val）
**訓練配置**: YOLOv8n, 50 epochs, 640x640, batch=16
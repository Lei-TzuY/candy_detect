# 模型位置總結

## ✅ 您訓練的糖果專用模型

經過檢查，找到以下糖果專用訓練模型（類別數 = 2）：

### 1. **datasets/最新資料集/best_candy_model.pt** ⭐ 推薦使用
   - **類別：** {0: 'abnormal', 1: 'normal'}
   - **大小：** 5.4 MB
   - **位置：** 最新資料集目錄
   - **狀態：** ✅ 當前配置已設置為此模型

### 2. runs/detect 目錄下的訓練模型
   - `runs/detect/runs/detect/candy_gpu_v1/weights/best.pt`
   - `runs/detect/runs/detect/candy_v1/weights/best.pt`
   - `runs/detect/runs/detect/candy_v2/weights/best.pt`
   - `runs/detect/runs/train/candy_detector/weights/best.pt`
   - `runs/detect/runs/train/candy_detector2/weights/best.pt`
   - `runs/detect/runs/train/candy_detector3/weights/best.pt`
   - `runs/detect/runs/train/candy_detector_yolov8s_max/weights/best.pt`
   
   這些也是有效的糖果訓練模型，可以在網頁界面中選擇測試。

## ❌ COCO 預訓練模型（僅用於訓練起點）

### 根目錄
```
yolo11m.pt, yolo11n.pt, yolo11s.pt
yolov8m.pt, yolov8n.pt, yolov8s.pt
```

### datasets/最新資料集/ 目錄
```
yolo11m.pt, yolo11n.pt, yolo11s.pt
yolov8m.pt, yolov8n.pt, yolov8s.pt
```

**這些模型：**
- 類別數：80（COCO 數據集：person, car, dog...）
- 用途：訓練新模型時作為起點（遷移學習）
- ❌ 不應直接用於糖果偵測

## 📋 文件組織結構

```
candy/
├── datasets/最新資料集/
│   ├── best_candy_model.pt          ✅ 糖果專用模型（當前使用）
│   ├── best_candy_model.onnx        ℹ️  ONNX 格式版本
│   ├── data.yaml                    ℹ️  訓練配置
│   ├── train/                       ℹ️  訓練數據集
│   ├── valid/                       ℹ️  驗證數據集
│   ├── test/                        ℹ️  測試數據集
│   ├── train_yolo11m.py             ℹ️  訓練腳本
│   ├── train_yolo11n.py
│   ├── ...
│   └── yolo*.pt                     ❌ COCO 預訓練模型
│
├── runs/detect/
│   ├── runs/detect/candy_*/weights/best.pt    ✅ 早期訓練模型
│   └── runs/train/candy_*/weights/best.pt     ✅ 訓練模型
│
├── yolo*.pt                         ❌ COCO 預訓練模型（根目錄）
│
└── config.ini                       ✅ 當前配置指向 best_candy_model.pt
```

## 💡 使用建議

### 方法 1：使用 best_candy_model.pt（當前配置）

**優點：**
- 最新訓練的模型
- 位於數據集目錄，便於管理
- 已在 config.ini 中配置

**使用：**
```ini
# config.ini
weights = datasets/最新資料集/best_candy_model.pt
```

### 方法 2：通過網頁界面切換模型

1. 打開 http://localhost:5000
2. 選擇 🤖 模型版本下拉選單
3. 選擇任何 `[已訓練]` 開頭的模型
4. 系統會自動更新 config.ini

### 方法 3：比較不同訓練版本

您有多個訓練的模型版本，可以測試哪個效果最好：
- candy_detector (最早版本)
- candy_detector2
- candy_detector3
- candy_detector_yolov8s_max
- best_candy_model（最新版本）⭐

建議使用 `best_candy_model.pt`，因為名稱暗示這是最佳版本。

## ⚠️ 重要注意事項

### 類別順序差異

注意！比較以下兩個模型的類別順序：

**best_candy_model.pt:**
```python
{0: 'abnormal', 1: 'normal'}  # abnormal 在前
```

**models/classes.txt:**
```
normal      # 0
abnormal    # 1
```

**如果類別順序不匹配，可能導致：**
- 正常品被標記為瑕疵 ❌
- 瑕疵品被標記為正常 ❌

**解決方案 1：** 更新 models/classes.txt 以匹配模型

```
abnormal
normal
```

**解決方案 2：** 重新訓練模型，確保類別順序一致

讓我檢查 data.yaml 確認正確的類別順序...

## 🔧 當前配置狀態

- ✅ `config.ini` 已更新為 `datasets/最新資料集/best_candy_model.pt`
- ⚠️ 需要確認類別順序是否匹配
- 🔄 重啟應用程式以應用新配置

## 📊 總結

| 模型位置 | 類別數 | 類別內容 | 用途 | 使用 |
|---------|--------|---------|------|------|
| datasets/最新資料集/best_candy_model.pt | 2 | abnormal, normal | 糖果偵測 | ✅ 當前 |
| runs/detect/.../best.pt (x7) | 2 | normal, abnormal | 糖果偵測 | ✅ 可用 |
| datasets/最新資料集/yolo*.pt | 80 | COCO 類別 | 訓練起點 | ❌ |
| 根目錄/yolo*.pt | 80 | COCO 類別 | 訓練起點 | ❌ |

## 下一步

1. **確認類別順序** - 檢查 data.yaml 和 classes.txt 是否匹配
2. **重啟應用程式** - 應用新的模型配置
3. **測試偵測** - 確認偵測框正常顯示
4. **（可選）整理檔案** - 將 COCO 預訓練模型移到專門的目錄避免混淆
